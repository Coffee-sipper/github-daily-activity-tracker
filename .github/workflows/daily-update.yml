name: Daily Activity Update

on:
  schedule:
    - cron: "30 19 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"

      - name: Run daily update
        run: python update.py

      - name: Commit changes
        run: |
          git config user.name "Coffee-sipper"
          git config user.email "${{ secrets.COMMIT_EMAIL }}"

          git add activity.log stats.json

          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "chore: record daily activity"
          git push
